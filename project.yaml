# // Auto-generated , DO NOT EDIT
specVersion: 1.0.0
name: near-subql-starter
version: 0.0.1
runner:
  node:
    name: '@subql/node-near'
    version: '*'
  query:
    name: '@subql/query'
    version: '*'
description: >-
  This is an example project that indexes price oracle feeds from the NEAR
  blockchain using SubQuery
repository: https://github.com/subquery/near-subql-starter
schema:
  file: ./schema.graphql
network:
  chainId: mainnet
  endpoint:
    - https://rpc.mainnet.near.org
  bypassBlocks:
    - 81003306
dataSources:
  - kind: near/Runtime
    startBlock: 125226210
    mapping:
      file: ./dist/index.js
      handlers:
        - handler: handleBlock
          kind: near/BlockHandler
        - handler: handleAction
          kind: near/ActionHandler
          filter:
            type: DeployContract
